# Ruby versions changelog

<%- ruby_versions.each do |ruby_version| %>

## Ruby <%= ruby_version['version'] %>

  <%- if !ruby_version['eol'].nil? %>
- All maintenance ended on <%= ruby_version['eol'] %>.
  <% end -%>

  <%- [*ruby_version['minors']].each do |minor_version| %>
### Ruby [<%= minor_version['version'] %>](https://www.ruby-lang.org/en/news/<%= minor_version['release_date'].gsub('-', '/') %>/ruby-<%= minor_version['version'].gsub('.', '-') %>-released/)

- Released on <%= minor_version['release_date'] %>
  <% end -%>

<% end -%>

(Last updated on <%= Time.now %>)
